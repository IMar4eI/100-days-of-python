# Day 27: Telemetry Scaler - Tkinter GUI & Variadic Arguments 

This repository contains the Day 27 architectural module from the "100 Days of Code" challenge.

## Technical Specifications
The code showcases basic event-driven GUI structures and evaluates variable-length function argument layouts.

* **Variadic Parameter Structs:** Implements standard dynamic unpacking models (`*args`, `**kwargs`) to mimic hardware configuration parameters.
* **Layout Management:** Uses coordinate grid positioning flags (`widget.grid()`) to map UI interaction components.

## Run
```bash
uv run main.py